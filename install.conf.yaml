- clean: ['~']

- link:
    ~/.config/alacritty: config/alacritty
    ~/.tmux.conf: config/tmux
    ~/.config/nvim/lua/custom: config/NvChad

- shell:
    - [git submodule update --init --recursive, Installing submodules]
