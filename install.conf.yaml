- clean: ['~']

- link:
    ~/.config/alacritty: config/alacritty
    ~/.tmux.conf: config/tmux/.tmux.conf
    ~/.config/nvim: config/nvim/lua/custom

- shell:
    - [git submodule update --init --recursive, Installing submodules]
